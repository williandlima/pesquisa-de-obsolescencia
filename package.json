{
  "name": "rastreador-obsolescencia",
  "version": "1.0.0",
  "private": true,
  "description": "Verificador de status de lifecycle (Active/NRND/Obsolete) de componentes eletrônicos, cruzando múltiplas fontes de distribuidor autorizado.",
  "scripts": {
    "test": "node tests/run.js",
    "test:backend": "node tests/run.js backend",
    "test:frontend": "node tests/run.js frontend",
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.56.1",
    "wrangler": "^4.0.0"
  }
}
